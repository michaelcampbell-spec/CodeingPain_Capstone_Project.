apiVersion: v1
kind: Service
metadata:
  name: website-service
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  selector:
    app: second-nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer

